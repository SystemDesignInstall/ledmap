import type { CabinetPixelLayoutConfig } from './pixel-layout.js'

export const referenceCabinetLayout: CabinetPixelLayoutConfig = Object.freeze({
  moduleColumns: 4,
  moduleRows: 4,
  modulePixelWidth: 32,
  modulePixelHeight: 32,
})
